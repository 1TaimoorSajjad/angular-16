import { TodayTripsPipe } from './today-trips.pipe';

describe('TodayTripsPipe', () => {
  it('create an instance', () => {
    const pipe = new TodayTripsPipe();
    expect(pipe).toBeTruthy();
  });
});
