import { HourPipePipe } from './hour-pipe.pipe';

describe('HourPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HourPipePipe();
    expect(pipe).toBeTruthy();
  });
});
