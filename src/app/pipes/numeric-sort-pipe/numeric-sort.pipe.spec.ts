import { NumericSortPipe } from './numeric-sort.pipe';

describe('NumericSortPipe', () => {
  it('create an instance', () => {
    const pipe = new NumericSortPipe();
    expect(pipe).toBeTruthy();
  });
});
