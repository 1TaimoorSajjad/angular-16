import { PoisitiveNumberDirective } from './poisitive-number.directive';

describe('PoisitiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PoisitiveNumberDirective();
    expect(directive).toBeTruthy();
  });
});
