import { ImagesliderDirective } from './imageslider.directive';

describe('ImagesliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesliderDirective();
    expect(directive).toBeTruthy();
  });
});
