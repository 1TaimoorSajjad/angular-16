import { ConvertPhoneNumberDirective } from './convert-phone-number.directive';

describe('ConvertPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
